preview:
<img width="1919" height="1019" alt="image" src="https://github.com/user-attachments/assets/a899394c-40f8-405e-907d-ee57edce671a" />
<img width="510" height="511" alt="image" src="https://github.com/user-attachments/assets/61e5fa80-d497-4126-ab60-b8d0c55b9f1e" />
<img width="1614" height="852" alt="image" src="https://github.com/user-attachments/assets/4fc8c318-5474-4eea-bd9c-17a32b0f5938" />

# MJRManager

**MJRManager** — мощный инструмент для работы с JAR-архивами: декомпиляция, редактирование и перекомпиляция Java-классов с современным графическим интерфейсом.

![Java](https://img.shields.io/badge/Java-21-orange)
![JavaFX](https://img.shields.io/badge/JavaFX-21.0.6-blue)

## 🚀 Возможности

- **📦 Открытие JAR/WAR/EAR архивов** — просмотр структуры файлов в удобном древовидном виде
- **🔍 Декомпиляция .class файлов** — автоматическая декомпиляция с использованием CFR Decompiler
- **✏️ Редактирование исходного кода** — встроенный редактор с подсветкой синтаксиса Java
- **⚙️ Перекомпиляция классов** — два компилятора на выбор:
  - **Eclipse JDT** (мягкий режим) — компилирует даже с ошибками
  - **javac** (строгий режим) — стандартный Java-компилятор
- **💾 Сохранение изменений** — обновление JAR-архива с автоматическим созданием резервной копии
- **🔎 Поиск по файлам** — быстрый поиск классов в дереве проекта
- **📜 История файлов** — список недавно открытых JAR-архивов
- **🐛 Консоль отладки** — встроенный логгер для отслеживания операций
- **👁️ Просмотр байткода** — возможность просмотра hex-дампа .class файлов

### ✨ Новые функции

- **🔍 Глобальный поиск** (`Ctrl+Shift+F`) — поиск текста во всех декомпилированных классах с поддержкой regex
- **📄 Редактор манифеста** — просмотр и редактирование META-INF/MANIFEST.MF
- **➕ Добавление/удаление файлов** — управление содержимым JAR-архива
- **📦 Экспорт в исходники** (`Ctrl+E`) — декомпиляция всех классов в структуру проекта

## 🔧 Установка и запуск

### Клонирование репозитория

```bash
git clone https://github.com/legkouz21-hash/mjrmanager.git
cd mjrmanager
```

## 🎯 Использование

1. **Открытие JAR-файла**
   - Меню: `Файл → Открыть JAR`
   - Или перетащите JAR файл в окно приложения

2. **Декомпиляция класса**
   - Выберите `.class` файл в дереве проекта
   - Класс автоматически декомпилируется и откроется в редакторе

3. **Редактирование кода**
   - Измените код в редакторе
   - Используйте подсветку синтаксиса для удобства

4. **Компиляция изменений**
   - Нажмите кнопку `Компилировать` или `Ctrl+Shift+C`
   - Выберите компилятор в меню `Редактор → Компилятор`

5. **Сохранение JAR**
   - `Файл → Сохранить` — перезаписать оригинальный файл (создаётся backup)
   - `Файл → Сохранить как...` — сохранить в новый файл

## 👨‍💻 Автор

**M1le** & **Kiro ai**

## 🆕 Последние изменения

- **🔍 Глобальный поиск** (`Ctrl+Shift+F`) — поиск по всем классам JAR с поддержкой regex
- **📄 Редактор манифеста** — просмотр и редактирование META-INF/MANIFEST.MF
- **➕ Управление файлами** — добавление и удаление файлов из JAR
- **📦 Экспорт в исходники** (`Ctrl+E`) — декомпиляция всех классов в структуру проекта
- **🗺️ Миникарта кода** — навигация по большим файлам как в VS Code
- **▶️ Запуск JAR** — встроенный запуск с выводом в консоль
- **🔥 Live режим (Hot-swap)** — изменение кода прямо в запущенном JAR без перезапуска
- **🔗 Зависимости классов** — анализ что использует класс и где он используется
- **🔄 Декомпилятор Procyon** — альтернатива CFR для сложных случаев
- **🧵 Fabric ремаппинг** — снятие intermediary маппингов через официальный TinyRemapper
- **🔎 Поиск и замена** (`Ctrl+F` / `Ctrl+H`) — поиск и замена внутри открытого файла
- **📋 Окно задач** — панель активных фоновых задач в стиле IntelliJ IDEA

⭐ Если проект оказался полезным, поставьте звезду!
